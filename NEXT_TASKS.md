# 작업 내역 · 남은 일

마지막 갱신: 2026-09-01 (신규 규칙 추가 계획 반영)

## 목표

행정력 최소화. "관리자가 시트를 눈으로 훑어 대상자를 찾아내는" 일을 없앤다.
자동 판정 · 자동 리포트 · 당사자 자가 확인.

## 원칙

- **입력은 익숙한 도구로, 조회·판단은 DB로**
  - 편성 → 시트에서 (드래그·복사가 웹 UI보다 빠름)
  - 김밥·과제 신청 → 구글 폼에서
  - 출석 체크 → 웹앱에서
- **출결의 원본은 시트다. DB 는 사본이다** ← 2026-08 에 뒤집었다
- **규칙은 SQL 뷰 한 곳에만** → 기준이 바뀌면 한 줄만 고친다
- **규칙 수정 비용 절감 (신규)**: 잦은 출석 규칙 변경에 따른 웹/DB 수정 리소스를 줄이기 위해, 출석 룰 엔진(Rule Engine) 연산은 구글 시트(GAS)에서 전담하고 DB/웹은 연산 결과만 수신하여 조회
- **결석자/수료자 관리는 DB 유지**: 출석 규칙 연산만 시트(GAS)로 이관하고, 기존의 결석자 관리 및 수료자 판정 로직은 DB/웹에서 그대로 수행

> 한때 DB 를 원본으로 두고 앱이 직접 썼다. 시트에서도 손으로 고치니 두 곳에서
> 쓰는 꼴이 됐고, 어느 쪽이 최신인지 판단할 근거가 없어 어긋났다.
> 시트에만 있던 ◎ 를 앱이 몰라 이수자가 결석으로 저장된 일이 실제로 있었다.
> 지금 흐름은 `CLAUDE.md` 에 있다.

---

## 확정된 규칙

```
분모: 실제 강의 16강 = 교리1~12 + 성경적대화1~4
      (교제·나눔은 is_class=false 로 제외)

출석 인정:  O (현장)  ·  ◎ (지난 기수 이수 이월)  ·  지각 출석 (지각 2회 후 소감문 대체)
지각:       지각 (1회는 출석 인정)  ·  지각(결석) (2회 누적 시 결석 처리)
결석:       X  ·  과제 (결석했지만 과제·소감문으로 메움)
집계 제외:  −  (하차·중도합류·휴강 등 사유 불문)
            빈칸 (아직 기록되지 않음 — 결석이 아니다)

[지각 규정 (신규 도입 예정)]
1. 지각 2회 = 결석 1회로 간주 (심방 및 과제 소감문 제출 대상)
2. 소감문 작성: 두 번째 지각한 강의(주차) 기준으로 작성 안내
3. 소감문 제출 시: 지각 2회 모두 '지각 출석'으로 인정 및 변경 (과제 대체와 동일하게 처리)

[보충 규정]
- 결석(X·과제) 주차에 과제·소감문 제출 시 출석 인정, 최대 3회
- '과제' 및 '지각 출석'은 사람이 읽으라고 붙이는 라벨이고, 인정 근거는 제출 기록이다

[수료 판정]
수료(O)      : credited(= present + makeup_used) >= 16
관리자확인   : 보충이 3회를 초과 (참작 사유 판단 필요)
미수료(X)    : 그 외

재수강       : 4회 결석 (새가족교육 안내문 4항)
△(부분수료)  : 기준 미확정. 시트 값만 보존하고 자동 판정에는 미사용
```

**출석 규칙 연산은 시트(GAS)에서 수행하며, 수료/결석자 판정 규칙은 `supabase/views.sql`에서 유지 관리한다.**

---

## 완료된 작업 내역

### 1~3단계 — 기반 · 판정 · 본인 안내  ✅

- 스키마·동기화·GitHub Actions (`sync-db` / `query-db` / `run-sql`)
- 판정 뷰 7종 (`v_completion_status` 외)
- 조회 화면의 진행률 카드 · 미제출 과제 안내

### 4단계 — 튜터·관리자 도구  ✅

| 화면 | 내용 |
|---|---|
| **출석 관리** | 주차·조 선택 → `O X` 입력 → 바뀐 사람만 저장. `◎ −` 는 보기 전용 |
| **결석 현황** | 이 주차 / 누적(2·3·4회 이상). 과제·소감문 대체분도 표시 · 담당교역자별 · 정렬 · 명단 복사 |
| **수료 현황** | 여섯 갈래 + 갈래 안 재분류(예정·진행 중은 '몇 회 더', 아슬아슬은 '과제 몇 건') · 할 일까지 |
| **출석부 출력** | 조별 A4 인쇄. 칸 토글 · 월말 김밥신청 자동 · 장별 출력 선택 · 집계표 |
| **개인별 보기** | 카드를 누르면 출석 그리드(과제·김밥 표시) · 김밥 · 과제 |
| **조별 보기** | 편성 확인 |
| **시트에서 지금 가져오기** | 관리자 화면 버튼 → GAS → GitHub Actions 동기화 |

### 4.5단계 — 기수 전환  ✅

`cohorts.is_active` 한 줄로 전환. 절차는 `COHORT_SWITCH.md`.
지난 기수 이수분(◎) 자동 이월 — `scripts/carry-over-attendance.mjs`.

### B안(DB 원본) 되돌리기  ✅

- 앱의 쓰기는 `sbPostGas` 로만 나간다. `sbRpc` 는 일부러 두지 않았다.
- GAS `doPost` → ① 출석부 탭(원본) ② `set_attendance_batch`(사본)
- 시트에 직접 입력한 것은 10분 트리거(`pushAttendanceToDb`)가 DB 로 밀어 올린다.
- `pullAttendanceFromDb`(DB → 시트)는 **기수 시작 때만** 실행.

### 5단계 — 튜터가 스스로 챙긴다  ✅

조원 명단 위 한 줄. 평소엔 숫자만, 누르면 챙길 사람과 **할 일**만 펼친다.

---

## 2026-08 에 잡은 것들 (주요 개선 내역)

| 무엇 | 왜 위험했나 / 개선 이유 |
|---|---|
| 출결 원본을 시트로 되돌림 | 두 곳에서 쓰다가 이수자(◎)가 결석으로 덮였다 |
| `◎ −` 를 입력 불가로 | 확인 대화상자는 그냥 누르기 쉬움. 구조적으로 차단 |
| 빈칸이 결석으로 저장되지 않던 문제 | 튜터가 출석자만 체크하면 결석이 누락되던 현상 수정 |
| 결석 집계를 기준 주차까지만 | 지난 기수 참석자의 앞 주차 X 가 잡혀 무결석자가 4회로 뜨던 문제 수정 |
| 명단 순서를 시트와 일치 (v105) | 종이·화면·시트 순서 불일치 해결 (`members.sheet_row`) |
| 인쇄 시 빈 종이 및 가운데 정렬 버그 (v105) | body flex 중앙정렬 해제로 1페이지 출력 정렬 정상화 |
| `과제` 상태값 분리 도입 (v104) | `◎`의 이월/대체 혼용 분리, 3회 보충 한도 우회 방지 |

---

## 작업 예정 내역 (남은 일)

### 🔴 1. 신규 출석 규칙 도입 및 GAS 연산 로직 이관 (신규 추가 ⭐)

- **규칙 내용**:
  1. 지각 2회 시 결석 1회 처리 (심방 및 과제 소감문 제출 대상)
  2. 과제 소감문은 **두 번째 지각한 강(주차)** 기준으로 안내
  3. 소감문 제출 시 지각 2회 모두 **`지각 출석`**으로 인정/변경
- **아키텍처 및 구현 방식**:
  - 규칙 수정 시 프로그램 수정 소요 자원 최소화를 위해 **출석 규칙 연산은 GAS(시트)가 전담**
  - 과제/소감문은 구글 폼으로 시트에 직접 적재되므로 단방향(시트 ➔ DB) 파이프라인으로 안전하게 연동
  - 구글 시트(GAS)에서 최종 상태값(`지각`, `지각(결석)`, `지각 출석` 등)을 산출하여 DB로 동기화
  - DB와 웹은 시트의 연산 결과만 받아오며, 기존의 결석자/수료자 관리 연산 로직은 DB/웹에 유지
- **세부 작업 항목**:
  - [ ] **DB 스키마/Enum 반영**: `지각 출석` 및 상태 매핑 추가
  - [ ] **GAS 연산 함수 작성**: 시트 내 지각 누적 카운트, 2회 시 대상 강 판정, 구글 폼 제출 매칭 후 '지각 출석' 변환 로직 구현
  - [ ] **정책 확정**: 지각 과제 소감문 제출이 기존 "결석 보충 3회 한도" 카운트에 포함되는지 여부 결정

---

### 🟡 2. 지금 사람이 해야 하는 것 (배포/운영 반영)

- [ ] **`sheet_row` 켜기 (v105)** — 조 순서를 시트대로 만드는 칸
  1. Supabase 에서 `supabase/add_sheet_row.sql` 실행 (`alter table` 한 줄)
  2. 관리자 화면 **⟳ 시트에서 지금 가져오기** 를 눌러 값을 채운다
  3. 그 전까지는 `nullslast` 로 예전 순서 그대로 유지
- [ ] **`과제` 값 켜기 (v104)** — 코드는 준비 완료, DB/GAS 반영 필요
  1. Supabase 에서 `supabase/views.sql` · `supabase/rpc_attendance.sql` 재실행
  2. GAS `doGet.js` · `pullAttendance.js` 반영 후 **기존 배포에서 '새 버전'으로 웹앱 재배포**
  3. 검증 쿼리로 `credited` 분포 대조
- [ ] **기존 `◎` 가르기 (선택)** — 손으로 `◎`로 적은 과제 대체분을 `과제`로 정리 (필요 시 일회용 스크립트 실행)
- [ ] **GAS 스크립트 속성 `GH_TOKEN` 설정** — 관리자 화면 동기화 버튼 동작용 fine-grained PAT (`Actions: Read and write`)
- [ ] **Git 과거 이력 개인정보 정리** — 과거 커밋 이력에 남아 있는 CSV 5개 정리 (저장소 Private 전환 또는 `git filter-repo`)
- [ ] **원격 브랜치 정리** — `claude/fix-attendance-cache-sKSlV` 브랜치 삭제

---

### 🟢 3. 신규 기능 개발 계획

- [ ] **김밥 신청 배너** — 매월 마지막 주에 앱 상단 배너 노출 및 구글 폼 연결 (※ 관리자용 김밥 전용 폼 생성 필요)
- [ ] **신규 신청자 목록 화면** — 교육신청 폼으로 들어온 미편성 인원 알림
- [ ] **조별 리포트** — 튜터별 대상자 요약 (결석/수료 현황 기반)
- [ ] **텔레그램 봇 주간 리포트** — 조별 텔레그램 방에 주간 요약 자동 게시

---

### ⚪ 4. 정책 미결정 / 검토 사항

- **`−`(수업 없음/중도합류) 포함자의 수료 분모 산정 기준**: 현재 16강 고정이라 `−`가 1개만 있어도 수료 불가 표시됨 → `required = 16 − skipped_count` 적용 여부 검토
- **수강생 시작 주차 표시**: 첫 출석 주차 표시 기능 도입 여부
- **`△`(부분수료) 판정 기준**: 확정 시 `v_completion_status` 뷰에 반영
- **김밥 신청 폼 항목 구성 및 리포트 발송 주기/시각 확정**
