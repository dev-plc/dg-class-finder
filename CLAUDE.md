# 이 저장소에서 일하는 규칙

**시작 전에 `docs/HANDOVER.md` 를 읽는다.** 지금 상태·남은 일·함정이 거기 있다.

1. **버전은 `npm run bump` 로만 올린다.** 손으로 `?v=` 를 붙이면 node 전용 파일
   (`tests/*.mjs` · `scripts/*.mjs`)에도 붙어 쓸모없이 바뀐 파일이 되고 검증이 깨진다.
2. **내보내기 전에 `npm test`** — 15개 스위트가 전부 통과해야 한다.
3. **출석·하차 규칙의 단일 출처는 `docs/RULES.md`.** 숫자를 고칠 일이 있으면
   그 문서를 먼저 고치고 코드를 고친다. 화면마다 표시가 다른 것은 버그가 아니다 —
   이유가 그 문서에 적혀 있다.
4. **GAS 재배포는 '배포 관리 → ✏️ → 새 버전'.** '새 배포' 를 누르면 URL 이 바뀌어
   GitHub Secrets 의 옛 URL 이 404 가 된다.
5. **매트릭스를 새 화면에서 쓸 때는 `getStatus` 를 넘긴다.** 안 넘기면 값이 한 열만
   차는데 오류가 안 난다. localStorage 캐시가 가리므로 **시크릿 창**에서 확인할 것.
6. 개인정보 — `service_role` 키는 GitHub Secrets 에만. 클라이언트에는 `anon` 키만.
   **공개 저장소이고 Actions 로그도 공개다** — 로그에 실명·전화를 찍지 않는다.

---

## 개발 기록 (옵시디언 연동)

이 저장소의 과거 개발 기록, 트러블슈팅 노트, 설계 결정 사항은 아래 경로에
정리되어 있습니다:

G:\내 드라이브\EleaZar\dev-notes\dg-class-finder\

과거에 겪었던 문제, 설계 이유, 트러블슈팅 이력이 필요한 작업을 할 때는
먼저 위 경로를 확인하세요. 각 노트에는 원본 GitHub 파일 경로와 링크가
프론트매터에 포함되어 있습니다.

그 폴더를 채우는 것은 `dev-plc/knowledge-base` 저장소입니다. main 에 .md 가
푸시되면 `.github/workflows/sync-obsidian.yml` 이 그리로 미러링하고, 데스크톱이
그 저장소를 위 경로로 `fetch` + `reset --hard` 해서 받아갑니다. **볼트에서 직접
고친 것은 다음 동기화 때 덮어쓰입니다** — 손으로 쓰는 메모는 `dev-notes` 바깥에
두세요. (예전에는 Actions 가 Google Drive API 로 직접 올렸는데, auth/drive 가
제한된 스코프라 OAuth 앱을 프로덕션으로 게시할 수 없고 테스트 상태의 리프레시
토큰이 7일마다 만료돼서 걷어냈습니다.)
